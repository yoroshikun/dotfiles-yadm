
complete -c fix -d "Fish POSIX Adapter"
complete -c fix -s a -d "Bind all variables, not just those which were exported"
complete -c fix -s f -d "Print bindings in fish syntax"
complete -c fix -s h -d "Print a help message"
complete -c fix -s p -d "Print bindings in sh syntax"
complete -c fix -s s -r -d "Specify a shell interpreter executable path"
complete -c fix -s t -d "Print the variables and aliases that would be created, but make no changes"
